class Node
!!!177026.cpp!!!	getInPin() : InPin
    return inPins;
!!!177154.cpp!!!	getID() : size_t
    return ID;
!!!177282.cpp!!!	addOutPin(inout endPoint : InPin) : void
    OutPin* nextOut = new OutPin();
    nextOut->connectToPin(endPoint);
    outPins.put(nextOut);
!!!177410.cpp!!!	executeFunction() : void
    Queue<OutPin> copy(outPins);
    while (!copy.isEmpty()) {
        OutPin* current = copy.get();
        current->setSignal(inPins[0].getSignal());
        current->sendSignal();
    }
